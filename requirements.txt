# Python 3.10-3.12 recommended (see README for the conda env).
# Core data / numerics
numpy>=1.24
pandas>=2.0
pyarrow>=14.0
scipy>=1.10
tqdm>=4.65
pyyaml>=6.0

# Modeling (frozen encoders cached offline; never fine-tuned)
torch>=2.1
torchaudio>=2.1
transformers>=4.40

# Audio I/O / resampling
soundfile>=0.12
librosa>=0.10

# Metrics / figures
scikit-learn>=1.3
matplotlib>=3.7
